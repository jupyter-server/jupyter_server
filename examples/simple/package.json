{
  "name": "jupyter-server-example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc -p src && webpack",
    "clean": "rimraf build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "webpack": "^5.72.0",
    "webpack-cli": "^5.0.0",
    "typescript": "~4.7.3"
  }
}
